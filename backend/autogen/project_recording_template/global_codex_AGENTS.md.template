# 全局 Codex 记录规则（Global Codex Recording Rules）

除非项目级 `AGENTS.md` 明确覆盖，否则在所有项目中应用以下规则。

## 启动阶段（Startup）

- 先检查项目是否已经存在记录骨架（recording skeleton）。
- 如果是新项目，在开展实质性工作前先初始化记录骨架。
- 如果是已有项目但还没有结构化记录，先补主要阶段总结，再切换到实时记录模式。

## 实时记录（Real-Time Recording）

- 每完成一个有意义的步骤，就追加一条结构化记录。
- 有意义的步骤包括：设计决策、代码修改、调试循环（debug cycle）、测试结果、运行结果和实验里程碑。
- 每条记录至少包含：目标、问题、根因、方案、验证、影响、下一步。

## 代码与文档（Code and Docs）

- 对关键代码路径和不直观逻辑增加教学式注释（teaching-oriented comments）。
- 当人工配置文件发生变化时，同步更新配置说明。
- 当出现长期有效的设计选择时，同步更新决策记录。
- 在面向人的文档中默认脱敏（redact）敏感信息。

## 文档分工（Document Ownership）

- `README.md` 只作为项目入口，不承担完整研发流水。
- `docs/project_context.md` 保存完整项目背景和上下文恢复信息。
- `docs/dev_journal.md` 是主时间线，记录问题、根因、方案、验证和影响。
- `docs/project_status.md` 只保存当前有效状态。
- `docs/decisions.md` 只记录长期有效的关键决策。
- `docs/config_guide.md` 记录所有可人工配置文件的字段含义和修改方法。
- `docs/learning_notes.md` 保存技术原理、踩坑和学习记录。
- `docs/case_study.md` 用于阶段性复盘、面试或论文素材，通常等用户明确触发。
- `docs/file_roles.md` 说明每个记录文件如何维护。
